# PROJ-X: Feature Name

<!-- This file (spec.md) is the stable CONTRACT — it defines WHAT, not HOW.
     Owner: /write-spec (creates), /refine (updates). During /build this file is READ-ONLY.
     Technical design lives in design.md, QA results in qa-report.md.
     No status or date fields here: the feature's status lives ONLY in features/INDEX.md,
     and git records when this file changed. A contract that is read-only during /build
     cannot carry a field that changes during /build. -->

## Dependencies
- None

## User Stories
- As a [user type], I want to [action] so that [goal]

## Out of Scope
<!-- What this feature explicitly does NOT cover. Critical for developer handoffs. -->
- _Example: Bulk operations (deferred to P1 — PROJ-X)_
- _Example: Admin view of other users' data (separate admin feature)_

## Acceptance Criteria

<!-- Write these in the project's working language (CLAUDE.md → Key Conventions).
     Deutsch: Angenommen [Vorbedingung], wenn [Aktion], dann [Ergebnis]
     English: Given [precondition], when [action], then [outcome]
     Use one of the two for every AC and EC in this file — never both.
     Each AC gets a stable ID (AC-1, AC-2, …). Tasks reference these IDs;
     qa-report.md reports per AC-ID. Chain: AC → Task → Test. -->

- [ ] **AC-1** — [Given/Angenommen …], [when/wenn …], [then/dann …]
- [ ] **AC-2** — [Given/Angenommen …], [when/wenn …], [then/dann …]

## Edge Cases
<!-- Each edge case gets a stable ID (EC-1, EC-2, …) so tasks and tests can reference it. -->
- **EC-1** — What happens when...?
- **EC-2** — How do we handle...?

## Technical Requirements (optional)
- Performance: < 200ms response time
- Security: Authentication required
- Browser Support: Chrome, Firefox, Safari

## Open Questions
<!-- Unresolved questions from the spec interview. Close them in /refine when answered. -->
- [ ] Question 1

## Decision Log
<!-- Record of conscious decisions made and why. Added to by /write-spec. -->

### Product Decisions
<!-- Added by /write-spec -->
| Decision | Rationale | Date |
|----------|-----------|------|
| _Example: Limit to 3 tags per item_ | _Keeps UI simple for MVP; can be raised later_ | YYYY-MM-DD |
